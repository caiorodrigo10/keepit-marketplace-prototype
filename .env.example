# ===========================================
# KEEPIT STOREFRONT - VARIÁVEIS DE AMBIENTE
# ===========================================
# Copie este arquivo para .env.local e preencha os valores

# ----- MEDUSA BACKEND -----
# URL do backend Medusa (produção: https://api.seudominio.com)
MEDUSA_BACKEND_URL=http://localhost:9000

# Publishable key do Medusa (encontre no dashboard admin)
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_xxx

# ----- STOREFRONT -----
# URL base do storefront (produção: https://seudominio.com)
NEXT_PUBLIC_BASE_URL=http://localhost:8000

# Região padrão (ISO-2 lowercase: br, us, eu)
NEXT_PUBLIC_DEFAULT_REGION=br

# ----- STRIPE (PAGAMENTOS) -----
# Chave pública do Stripe (pk_live_xxx para produção)
NEXT_PUBLIC_STRIPE_KEY=

# Medusa Payments (encontre no dashboard)
NEXT_PUBLIC_MEDUSA_PAYMENTS_PUBLISHABLE_KEY=
NEXT_PUBLIC_MEDUSA_PAYMENTS_ACCOUNT_ID=

# ----- NEXT.JS -----
# Secret para revalidação on-demand (gere uma chave aleatória)
REVALIDATE_SECRET=gerar-chave-segura-aleatoria

# ----- MEDUSA CLOUD (OPCIONAL) -----
# Para usuários do Medusa Cloud
MEDUSA_CLOUD_S3_HOSTNAME=
MEDUSA_CLOUD_S3_PATHNAME=

# ----- GOOGLE MAPS -----
# Chave da API do Google Maps (configure restrições de domínio!)
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=

# ===========================================
# INSTRUÇÕES PARA DEPLOY NA VERCEL
# ===========================================
# 1. Acesse: https://vercel.com/[seu-projeto]/settings/environment-variables
# 2. Adicione cada variável acima com os valores de produção
# 3. Certifique-se de NÃO usar localhost em produção
# 4. Configure restrições de domínio na chave do Google Maps
